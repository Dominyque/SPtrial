<% provide(:title, @user.name) %>
<br>time is:<%= Time.now%>
<div class="row">
	<aside class="col-md4">
		<section class="user_info">
			<%= @user.name %>, <%= @user.email %>, created:<%= @user.created_at %>,updated: <%= @user.updated_at %>
			<h2>
				<%= gravatar_for(@user) %>
				<%= @user.name %>
			</h2>
		</section>
	</aside>
</div>
